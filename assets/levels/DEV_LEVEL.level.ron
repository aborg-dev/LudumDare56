LevelDefinition(
    creatures: [
        // CreatureDefinition (
        //     max_speed: 400.0,
        //     movement: Constant ( speed: Vec2(1.0, 1.0) )
        // ),
        CreatureDefinition (
            image: Duck,
            max_speed: 400.0,
            movement: Periodic ( max_speed: Vec2(1.0, 1.0), duration_ms: 500 )
        ),
        CreatureDefinition (
            max_speed: 1500.0,
            movement: Circle ( radius: 0.5, duration_ms: 4000 )
        ),
    ]
)